# Baby Time MVP 任务拆分与测试计划

## 1. 拆分原则

本任务文档用于后续分步开发、测试和上线。每个任务必须同时交付功能代码、自动化测试和必要文档更新。

上线规则：

- 未补充自动化测试的功能不得合并。
- 未通过全部测试的功能不得上线。
- 数据库 schema 变更必须包含迁移文件和迁移测试。
- API 行为变更必须同步更新 OpenAPI。
- 移动端和服务端必须通过 CI 后才能进入测试环境。

低成本 MVP 默认技术边界：

- 客户端：Expo + React Native + TypeScript。
- 服务端：NestJS 模块化单体。
- 数据库：PostgreSQL。
- ORM：Prisma。
- 媒体保存：本机索引模式 + 用户授权云端备份模式，Baby Time 后端默认不保存儿童照片原文件。
- 队列：MVP 先用本地可恢复队列 + PostgreSQL `media_backup_tasks` 记录云端备份状态，不部署 Redis/BullMQ。
- 搜索：MVP 先用 PostgreSQL 查询和索引，不接独立搜索服务。
- CDN、短信登录、AI、视频、家庭协作、音频转文字均不进入 MVP。

## 2. 测试体系任务

### T00.1 建立测试与 CI 基础

目标：

- 建立后端、客户端、数据库迁移、E2E 的自动化测试框架。
- 建立 CI 流水线，所有分支合并前必须跑测试。

开发内容：

- 后端配置 Jest 或 Vitest。
- 客户端配置 Jest + React Native Testing Library。
- API 集成测试使用测试数据库。
- E2E 测试使用 Detox 或 Maestro，MVP 优先 Maestro 以降低维护成本。
- CI 执行 typecheck、lint、unit test、integration test、migration test。
- 预留上线前 smoke test 脚本。

验收标准：

- 空项目测试流水线可稳定运行。
- CI 任一测试失败时阻止合并。
- README 或开发文档说明本地测试命令。

必须测试：

- 示例后端单元测试。
- 示例 API 集成测试。
- 示例客户端组件测试。
- 示例 E2E smoke test。
- Prisma migration 空库执行测试。

## 3. V0.1 基础工程与账号闭环

### T01.1 初始化客户端工程

目标：

- 创建 Expo React Native App，完成基础导航和工程结构。

开发内容：

- 建立底部导航：时间线、相册、录音、搜索、我的。
- 建立登录态路由和主应用路由。
- 配置 TypeScript、lint、format、测试命令。
- 建立 API client、错误处理、环境变量读取。

验收标准：

- App 可启动。
- 登录态和未登录态路由可切换。
- 底部导航页面可访问。

必须测试：

- 路由守卫逻辑单元测试。
- API client 错误映射测试。
- 底部导航渲染测试。

### T01.2 初始化后端工程

目标：

- 创建 NestJS API 服务，完成基础模块、配置和健康检查。

开发内容：

- 建立配置模块、日志模块、全局错误处理。
- 接入 Prisma 和 PostgreSQL。
- 提供 `GET /health`。
- 建立 OpenAPI 输出。
- 建立统一响应错误码和 request id。

验收标准：

- API 服务可本地启动。
- `GET /health` 返回正常。
- Prisma 可连接测试数据库。

必须测试：

- 配置加载单元测试。
- 健康检查 API 测试。
- 全局错误处理测试。
- 数据库连接与迁移测试。

### T01.3 账号注册登录

目标：

- 完成低成本 MVP 账号体系。

开发内容：

- 支持邮箱 + 密码注册登录。
- 密码使用 bcrypt/argon2 哈希。
- 使用 access token + refresh token。
- 支持退出登录和 refresh token 失效。
- 预留手机号和第三方登录字段，但不实现。

验收标准：

- 用户可注册、登录、刷新 token、退出登录。
- 未登录用户无法访问受保护接口。
- 密码不以明文保存。

必须测试：

- 注册成功/重复邮箱/弱密码测试。
- 登录成功/密码错误/用户不存在测试。
- token 过期和刷新测试。
- 受保护接口未登录访问测试。
- refresh token 退出后失效测试。

### T01.4 孩子档案

目标：

- 支持创建、查看、编辑、删除孩子档案，并选择默认媒体保存方式。

开发内容：

- 后端实现 children CRUD。
- 客户端实现孩子档案创建页、选择页、编辑入口。
- 创建孩子档案时提供本机索引、授权云端备份、稍后设置选项。
- 保存 `default_media_storage_mode` 和可选 `cloud_provider_account_id`。
- 当前孩子保存在本地状态。
- 根据生日计算年龄和月龄。

验收标准：

- 用户可管理自己的孩子档案。
- 用户不能访问其他用户的孩子档案。
- 删除孩子档案前二次确认。

必须测试：

- 孩子档案 CRUD API 测试。
- 越权访问测试。
- 年龄/月龄计算单元测试。
- 客户端表单校验测试。

## 4. V0.2 照片导入、云端备份与时间线

### T02.1 本机索引照片导入

目标：

- 实现不上传照片原文件的本机索引模式。

开发内容：

- 使用系统照片选择器。
- 记录系统相册资产标识、拍摄时间、mime type 等基础元数据。
- 后端创建照片索引记录，不接收照片原文件。
- 展示照片时根据资产标识重新向系统相册请求图片。
- 处理原照片删除、相册权限撤销和有限照片访问移除状态。

验收标准：

- 用户可批量选择照片并导入。
- Baby Time 后端不保存照片原文件。
- 系统相册原图删除后，App 展示照片缺失提示。
- 相册权限失效后，App 展示重新授权入口。

必须测试：

- 照片导入参数校验测试。
- 本机资产标识保存测试。
- 未授权和越权导入测试。
- 原照片缺失状态测试。
- 有限照片访问移除状态测试。

### T02.2 授权云端备份

目标：

- 支持至少一种主流云端存储服务授权，并把照片备份到用户授权目录。

开发内容：

- 接入 iCloud Drive、Google Drive、OneDrive 或 Dropbox 中至少一种。
- 实现云端授权、断开授权和授权失效处理。
- 按 `BabyTime/{child_id}/{yyyy}/{MM}/` 规则命名和上传照片。
- 保存云端服务类型、文件 ID、路径和同步状态。
- 实现云端备份队列状态：pending、uploading、synced、failed、auth_expired、quota_exceeded、missing。
- 支持失败重试、弱网提示和进度展示。

验收标准：

- 用户可授权云端服务。
- 用户可批量备份照片到自己的云端目录。
- 云端备份进度可见。
- 授权过期、空间不足、文件缺失时有明确提示。
- Baby Time 后端不保存第三方云端 token 明文。

必须测试：

- 云端授权状态测试。
- 云端备份队列 reducer/state machine 测试。
- 备份成功、失败、重试测试。
- 授权过期和空间不足错误映射测试。
- 云端备份 UI 状态组件测试。

### T02.3 照片展示与缺失状态

目标：

- 根据媒体保存方式展示照片，并处理缺失状态。

开发内容：

- 本机索引照片从系统相册读取缩略图和详情图。
- 云端备份照片优先使用本机引用展示；本机原图缺失时尝试从授权云端读取。
- 缺失状态区分本机原图不存在、相册权限失效、云端文件缺失、云端授权过期。
- 提供重新授权、重新备份、替换照片、保留缺失记录和移除索引入口。

验收标准：

- 照片列表和详情页可按不同保存方式正常展示。
- 缺失状态有明确占位和文案。
- 移除 Baby Time 索引默认不删除系统相册原图和用户云盘文件。

必须测试：

- 本机索引照片展示测试。
- 云端备份照片展示测试。
- 原照片缺失测试。
- 云端授权过期测试。
- 删除索引不删除外部文件测试。
- 对象存储写入 mock 测试。

### T02.4 时间线计算与展示

目标：

- 实现按孩子生日归档的时间线。

开发内容：

- 后端实现时间线节点计算。
- 照片按拍摄时间推荐时间节点。
- 提供时间线摘要接口。
- 客户端实现时间线首页和节点详情。

验收标准：

- 时间线显示节点标题、封面、照片数量、最近更新。
- 用户可查看节点下照片列表。
- 时间线首屏接口只返回摘要，不返回大图详情。

必须测试：

- 出生当天、第 1 周、月龄节点计算测试。
- 跨年生日和未来拍摄时间测试。
- 时间线摘要 API 测试。
- 时间节点详情分页测试。
- 客户端时间线空状态和有数据状态测试。

## 5. V0.3 照片集、tag 与分类

### T03.1 照片详情与备注

目标：

- 支持查看和编辑单张照片基础信息。

开发内容：

- 照片详情接口。
- 编辑标题、备注、时间节点。
- 客户端照片详情页。
- 删除照片二次确认。

验收标准：

- 用户可查看缩略图和展示图。
- 用户可编辑备注。
- 删除照片后不再出现在列表。

必须测试：

- 照片详情 API 测试。
- 编辑备注和时间节点测试。
- 删除照片软删除测试。
- 越权访问测试。

### T03.2 照片集

目标：

- 支持创建和管理照片集。

开发内容：

- 创建照片集。
- 添加、移除、排序照片。
- 设置封面。
- 保存基础 `layout_template` 和 `layout_data`。
- 客户端照片集详情页。

验收标准：

- 用户可从多张照片创建照片集。
- 用户可调整顺序和封面。
- 删除照片集时可选择是否保留单张照片。

必须测试：

- 照片集 CRUD API 测试。
- 照片排序测试。
- 封面设置测试。
- 删除照片集保留/删除照片测试。
- 客户端排序状态测试。

### T03.3 tag 管理与绑定

目标：

- 支持自定义 tag 和内容绑定。

开发内容：

- tag CRUD。
- tag 绑定照片、照片集。
- 按 tag 筛选照片和照片集。
- 客户端 tag 管理页和绑定控件。

验收标准：

- 用户可创建、编辑、删除 tag。
- 删除 tag 不删除内容。
- 用户只能使用自己的 tag。

必须测试：

- tag CRUD API 测试。
- tag 绑定/解绑测试。
- 删除 tag 后内容仍存在测试。
- 按 tag 筛选测试。
- tag 名称重复规则测试。

### T03.4 分类管理与绑定

目标：

- 支持系统分类和用户自定义分类。

开发内容：

- 初始化内置分类。
- 分类 CRUD。
- 分类绑定照片、照片集。
- 按分类筛选。

验收标准：

- 默认分类可用。
- 用户可创建自定义分类。
- 删除分类不删除内容。

必须测试：

- 内置分类初始化测试。
- 分类 CRUD API 测试。
- 分类绑定/解绑测试。
- 按分类筛选测试。
- 越权分类绑定测试。

## 6. V0.4 录音、音频与绑定

### T04.1 客户端录音

目标：

- 支持录音、暂停、继续、试听、重录和保存。

开发内容：

- 麦克风权限申请。
- 录音状态机。
- 本地试听。
- 录音文件临时保存。
- 录音异常提示。

验收标准：

- 用户主动录音时才申请麦克风权限。
- 用户可完成一段录音并试听。
- 用户可重录。

必须测试：

- 录音状态机测试。
- 权限拒绝状态测试。
- 暂停/继续/结束流程测试。
- 试听入口状态测试。

### T04.2 音频上传与播放地址

目标：

- 支持音频文件上传、保存和鉴权播放。

开发内容：

- 音频上传 intent。
- 保存音频元数据：duration、mime_type、title、note、audio_type。
- 生成短期签名播放 URL。
- 客户端播放进度、暂停、继续。

验收标准：

- 用户可上传录音并播放。
- 播放地址不是永久公开 URL。
- 音频只能被所属用户访问。

必须测试：

- 音频上传 intent 测试。
- 音频元数据校验测试。
- 播放签名 URL 权限测试。
- 音频播放组件状态测试。

### T04.3 音频绑定照片或照片集

目标：

- 支持音频与照片、照片集绑定。

开发内容：

- `audio_bindings` API。
- 照片详情页显示绑定音频列表。
- 照片集详情页显示绑定音频列表。
- 支持解绑和删除音频。

验收标准：

- 一段音频可绑定到照片或照片集。
- 解绑不删除原音频。
- 删除音频会移除绑定。

必须测试：

- 音频绑定/解绑 API 测试。
- target_type 枚举校验测试。
- 绑定目标归属校验测试。
- 删除音频级联绑定测试。
- 客户端绑定列表渲染测试。

## 7. V0.5 搜索、稳定性与上线准备

### T05.1 基础搜索

目标：

- 支持 MVP 范围内的基础搜索。

开发内容：

- 搜索照片备注。
- 搜索照片集标题和备注。
- 搜索 tag、分类。
- 搜索音频标题和备注。
- 搜索结果区分类型。

验收标准：

- 用户可搜索自己的内容。
- 搜索结果显示类型、标题、摘要和入口。
- 空结果有明确状态。

必须测试：

- 多类型搜索 API 测试。
- 只返回当前用户/当前孩子数据测试。
- 空关键词和特殊字符测试。
- 客户端搜索结果渲染测试。

### T05.2 删除、数据保护与账号注销

目标：

- 完成基础隐私与数据删除能力。

开发内容：

- 照片、照片集、音频软删除。
- 账号注销入口。
- 个人数据删除任务。
- 对象存储删除补偿任务。
- 敏感操作审计日志。

验收标准：

- 用户可删除个人内容。
- 账号注销后用户无法登录。
- 数据删除失败时有补偿记录。

必须测试：

- 软删除查询过滤测试。
- 账号注销测试。
- 对象删除失败补偿任务测试。
- 审计日志写入测试。

### T05.3 上线前 E2E 回归

目标：

- 验证核心闭环完整可用。

开发内容：

- 建立测试环境。
- 准备 E2E 测试账号、测试相册素材和测试云端备份账号。
- 编写核心路径自动化测试。
- 输出上线检查报告。

验收标准：

- 核心路径 E2E 全部通过。
- 测试环境数据可重置。
- 上线检查项全部通过。

必须测试：

- 注册登录。
- 创建孩子档案。
- 选择媒体保存方式。
- 导入照片。
- 云端备份照片。
- 创建照片集。
- 创建 tag 并绑定。
- 录音并绑定照片集。
- 时间线查看。
- 搜索内容。
- 删除内容。

## 8. V1.0 上线准入清单

功能准入：

- V0.1 至 V0.5 所有任务完成。
- 所有 P0 功能有自动化测试。
- 所有隐私和权限校验测试通过。
- 所有上传、删除、绑定、搜索核心流程通过 E2E。

质量准入：

- 后端单元测试通过。
- 后端 API 集成测试通过。
- 客户端单元/组件测试通过。
- E2E 冒烟测试通过。
- 数据库迁移测试通过。
- TypeScript 类型检查通过。
- Lint 通过。
- 无 P0/P1 级别未修复缺陷。

成本准入：

- 未部署 Redis、独立搜索、CDN、转码服务等非必要组件。
- 对象存储生命周期规则已配置。
- 日志保留周期已配置，避免无限增长。
- 测试环境和生产环境资源规格已分离。

安全准入：

- 对象存储桶为私有。
- 所有下载使用短期签名 URL。
- 所有业务资源按 user_id 和 child_id 校验。
- 删除和注销有二次确认。
- 隐私政策、用户协议准备完成。

## 9. 后续扩容任务池

当用户规模增长后，按触发条件追加以下任务：

- C01 引入 Redis + BullMQ：上传任务积压、缩略图处理影响 API 响应时启动。
- C02 拆分独立 Worker：媒体处理 CPU 占用 API 服务资源时启动。
- C03 接入 CDN：媒体访问慢或流量成本上升时启动。
- C04 引入独立搜索服务：PostgreSQL 搜索延迟无法接受时启动。
- C05 音频统一转码：音频播放兼容问题增多时启动。
- C06 数据库升级和连接池：数据库连接数或 CPU 接近上限时启动。
- C07 多区域对象存储或备份：用户地域扩展或备份要求提高时启动。
